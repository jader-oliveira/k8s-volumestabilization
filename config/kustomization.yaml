apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: volume-stabilization

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - deployment.yaml

images:
  - name: ghcr.io/yourusername/volume-stabilization-operator
    newTag: v0.1.0

labels:
  - pairs:
      app.kubernetes.io/name: volume-stabilization-operator
      app.kubernetes.io/managed-by: kustomize
